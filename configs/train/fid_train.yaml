# configs/train/fid_train.yaml
model:
  name: "google/flan-t5-large"
  batch_size: 4
  grad_accum: 8
  lr: 3e-5
  epochs: 5
  max_input_length: 1024
  max_output_length: 512

data:
  dataset: "govreport"
  split: "train"

retriever:
  k_final: 20

output_dir: "checkpoints/fid_govreport_final"